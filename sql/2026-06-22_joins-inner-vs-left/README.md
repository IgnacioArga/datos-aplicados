# INNER JOIN vs LEFT JOIN

Ejemplo reproducible de la diferencia entre `INNER JOIN` y `LEFT JOIN`, con la analogía del asado (invitados vs los que confirmaron que traen algo).

**Requisitos:** SQLite (`sqlite3`).
**Cómo correr:** `sqlite3 < joins_inner_vs_left.sql`

🔗 Post de LinkedIn: _pendiente de publicación._
